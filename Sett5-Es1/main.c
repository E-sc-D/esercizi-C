#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void FabargeEgg() {
    float r1, r2, r3;   // tre numeri reali
    float omega;

    omega =


}

float generaFloat(float min, float max) {
    float scale = rand() / (float) RAND_MAX;
}

int main() {
    srand(time(NULL));

    FabargeEgg();

    return 0;
}

